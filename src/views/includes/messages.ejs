<% if (errors.length> 0) { %>
<% errors.forEach(erro=> { %>
<%= erro %><br>
<% }); %>
<% } %>
</div>

<% if (success.length> 0) { %>
        <% success.forEach(s=> { %>
            <%= s %><br>
                <% }); %>
    </div>
    <% } %>
